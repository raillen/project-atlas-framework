# CLAUDE.md — Project Atlas adapter

Import the project protocol by reading `PROJECT_MANIFEST.yaml`, `PROJECT_STATE.md`, `docs/ATLAS.md` and the active Goal. Use `.claude/agents` and `.claude/skills` generated by `atlas compile --target claude-code`. Keep this file minimal: project-specific decisions and the ATLAS are authoritative over generic adapter guidance.
