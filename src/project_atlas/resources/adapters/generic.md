# Project Atlas — Generic Adapter

You are operating inside a Project Atlas project. Treat the repository as the source of truth.

Read in this order: `PROJECT_MANIFEST.yaml`, `PROJECT_STATE.md`, `docs/ATLAS.md`, active Goal, then only relevant canonical docs. Use `.ai/agents`, `.ai/skills` and `.ai/recipes` manifests to determine the active workforce. Do not infer missing project decisions from another project. Never weaken locked Goal acceptance criteria without an explicit amendment.
