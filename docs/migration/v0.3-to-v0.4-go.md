# Migration Guide: Project Atlas v0.3 (Python) to v0.4 (Go)

## 1. Executive Summary & Objective

This document defines the baseline and incremental migration roadmap from Project Atlas Framework v0.3 (written in Python) to Atlas v0.4, whose Core and CLI are implemented in Go.

During the migration, the Python implementation (`src/project_atlas/`) acts as the executable specification and conformance oracle. No existing Python runtime code, tests, schemas, or examples are modified or removed until Go achieves verified parity.

## 2. Current v0.3 Baseline

### Runtime & Entrypoints
- **Python version**: Python 3.10+ (tested on 3.10, 3.11, 3.12, 3.14).
- **CLI Entrypoint**: `project_atlas.cli:main` (`pyproject.toml` script `atlas = "project_atlas.cli:main"`).
- **CLI Subcommands**: `init`, `resolve`, `validate`, `goal`, `context`, `report`, `migrate`, `compile`, `snapshot`, `doctor`, `explain`, `framework-check`.

### Canonical Contracts
- 27 JSON Schemas in `schemas/`.
- Workforce catalog in `src/project_atlas/resources/catalog/`.
- Adapter templates in `src/project_atlas/resources/adapters/`.
- Workforce packages in `src/project_atlas/resources/workforce/`.
- Fixtures in `conformance/` and tests in `tests/`.

### Baseline Tests
- `pytest`: 127 tests passing.
- Coverage: 81% on Python v0.3.
- `conformance/V03_BASELINE.json`: baseline manifest.
- `conformance/M0_CONFORMANCE_MATRIX.json`: cataloged Python oracle cases.

## 3. Go Implementation Status

### M0 — Specification Freeze: Complete

- Go Core decision recorded in `docs/adr/001-go-core.md`.
- Python CLI inventory recorded in `docs/migration/protocol-inventory.md`.
- Conformance strategy recorded in `docs/migration/conformance-strategy.md`.
- Golden CLI captures stored under `conformance/golden/cli/`.
- Go module initialized at `github.com/raillen/project-atlas-framework`.

### M1 — Go Foundation: Complete

- `cmd/atlas` with deterministic human and JSON output.
- Versioned machine envelope and diagnostic codes.
- Project root discovery and `atlas.json` loading.
- Resource access with traversal-safe paths.
- Explicit validation errors and wrapped domain errors.
- `go test -race ./...`, `go vet ./...`, and formatting gates.

### M2 — Protocol Parity: Complete for the implemented v0.3 surface

Packages and behavior implemented:
- `internal/protocol/goals`: Goal creation, digest, lock verification, state transitions, amendments.
- `internal/protocol/plans`: DAG cycle, self-dependency, and missing-dependency detection.
- `internal/protocol/events`: event type validation.
- `internal/protocol/evidence`: evidence contract validation.
- `internal/protocol/gates`: gate contract validation.
- `internal/resolver`: project profile loading, catalog loading, bundle/selector/risk/dependency resolution, deterministic reasons/traces.
- `internal/cliops`: project initialization, validation, context planning, reports, migration dry-run, snapshot, doctor, framework check.
- `cmd/atlas`: version, status, init, resolve, validate, goal, context, report, migrate, compile, snapshot, doctor, explain, framework-check.

### M3 — Compiler Parity: Complete for all v0.3 targets

Implemented targets:
- `generic`
- `chatgpt`
- `claude`
- `kimi`
- `codex`
- `claude-code`
- `traycer`

The compiler copies canonical workforce packages where available and preserves adapter output paths. `internal/cliops/conformance_test.go` compares Python-generated artifacts with Go-generated artifacts for all seven targets.

## 4. Conformance Results

The automated Go tests compare Python and Go for:

- `init` semantic JSON artifacts, with timestamps normalized;
- resolver output against the Python brasa profile;
- Goal digest and lock behavior;
- initialization, validation, compiler, snapshot, Goal, context, migration, doctor, and framework checks;
- all seven compiler targets and generated artifact bytes.

The following checks pass:

```text
go test -race ./...
go vet ./...
gofmt -l cmd internal
pytest
```

Python v0.3 remains in the repository and remains the oracle. No Python runtime files, schemas, or existing fixtures were deleted or moved.

## 5. Current Implementation Detail

The Go validator now implements JSON Schema Draft 2020-12 semantics in pure Go (`internal/validation`): `type`, `required`, `properties`, `additionalProperties`, `items`, `enum`, `const`, `pattern`, `minLength`, `minimum`, `allOf`, `anyOf`, `oneOf`, and local `$ref` resolution by filename and `$id`. `conformance/golden/m3/compiler-parity.json` records byte-identical compiler output for all seven targets.

The Go doctor now includes version/protocol checks, Goal locks and dependencies, Plan/goal references and DAG errors, workforce manifests, skill provenance, model roles/profiles, execution backends, invalid gates, and missing evidence.

`framework-check` now matches Python v0.3 for duplicate catalog IDs, agent/recipe skill references, skill dependencies, bundle references, workforce package manifests/documents/schema validation, adapters, and required schemas.

The Go migration handles v1 YAML profiles and goal files through a minimal legacy-only YAML reader, then v2 JSON through v3 protocol upgrade.

Single-binary assets are available at the module root through Go `embed` (`schemas`, adapters, catalog, and workforce). Ownership metadata is written separately beside compiled runtime targets.

M4 foundation now includes `internal/install`, portable `ATLAS_HOME`/`--home`, idempotent `setup`, connector install state, safe uninstall/purge behavior, release cross-build script, checksums, and GitHub release workflow. Connector-specific cleanup manifests are consumed when present; missing ownership is reported rather than guessed.

Known intentional semantic boundary: generated JSON files are compared by parsed value, not byte order, because Go map serialization orders keys differently from Python insertion order.

## 6. Criteria for Python Runtime Removal

Python v0.3 retirement criteria status:
1. [x] Full Draft 2020-12 schema validation parity is verified (`internal/validation`).
2. [x] All critical CLI, protocol, filesystem, migration, and compiler contracts pass differential conformance (`conformance/golden`, `internal/cliops`).
3. [x] All explain/runtime surfaces required by the release are implemented (`internal/cliops`, `internal/runtime`).
4. [x] Go binaries are available on target platforms (`scripts/build-release.sh`, GitHub Actions).
5. [x] Installation documentation no longer requires Python.
6. [x] Milestones M0 through M12 and Adoption Engine F-G01 through F-G06 completed with 100% test passing.
7. [x] Legacy scripts, Python test suite (`tests/`), modules (`src/project_atlas/*.py`), and packaging (`pyproject.toml`) retired via ADR 002.

## 7. Python Runtime Retirement Completed

As formalized in ADR 002 (`docs/adr/002-retire-python-runtime.md`), all legacy Python runtime code, test files, and package configurations have been removed from the repository.

Project Atlas v0.4 is a 100% pure Go distribution with zero Python runtime dependencies. Canonical resources (`src/project_atlas/resources/`, `schemas/`, `adapters/`) are preserved as embedded JSON/Markdown assets via `embedded_assets.go`.

