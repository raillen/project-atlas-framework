# Execution Policy

Execution Policy (`schemas/execution-policy.schema.json`) answers **HOW to reach the model/agent**:
- Configures backends: `direct`, `gateway`, `cli`, `acp`, `custom`.
- Provider-neutral: works with local models, CLI harnesses, and remote gateways.
