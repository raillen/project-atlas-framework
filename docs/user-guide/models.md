# Model Policy v2

Model Policy (`schemas/model-policy.schema.json`) answers **WHICH model to use**:
- Defines profiles (capabilities, cost class, context window).
- Maps roles to preferred models or profiles.
- Enforces cross-provider review policies.
